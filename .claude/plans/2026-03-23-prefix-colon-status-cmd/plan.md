# Implementation Plan: Colon prefix + no auto-advance + /where command

> Generated by /ww-plan on 2026-03-23

## Steps

### Step 1: Change prefix separator from hyphen to colon in install.sh

- **Description**: Change `prefix-plan` → `prefix:plan` for filenames and all sed replacements. Update config file format. Handle re-install cleanup of old hyphen-style files.
- **Files**: install.sh
- **Complexity**: Medium
- **Depends on**: none

### Step 2: Add /where command

- **Description**: New command that reads state.json and shows current phase, progress, and what to do next.
- **Files**: commands/where.md
- **Complexity**: Low
- **Depends on**: none

### Step 3: Add no-auto-advance rule to all commands and CLAUDE.md

- **Description**: Add explicit instruction at the end of each command: "Do NOT auto-advance to the next phase. Suggest the next command but wait for the user to invoke it." Also add as a rule in CLAUDE.md.
- **Files**: commands/plan.md, commands/implement.md, commands/debug.md, commands/done.md, CLAUDE.md
- **Complexity**: Low
- **Depends on**: none

### Step 4: Update README

- **Description**: Document colon prefix format, /where command, and no-auto-advance behavior.
- **Files**: README.md
- **Complexity**: Low
- **Depends on**: Steps 1-3

## Risks

| Risk | Severity | Mitigation |
|------|----------|------------|
| Colon in filenames may break on some Linux/CI systems | Low | macOS tested OK; colon is POSIX-valid in filenames |
| Old hyphen-style installs need migration | Low | Re-install detects old config and cleans up |
